/** Schedule block for akad / resepsi (env-driven copy, Indonesian defaults). */
export type TEventScheduleBlock = {
  title: string;
  date: string;
  time: string;
  venue: string;
  mapUrl: string;
};
